name: changa_app
description: Conecta con changas cerca tuyo
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.4
  firebase_storage: ^12.3.3
  provider: ^6.1.5
  http: ^1.3.0
  shared_preferences: ^2.3.3
  image_picker: ^1.1.2
  google_maps_flutter: ^2.5.3
  geolocator: ^11.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
