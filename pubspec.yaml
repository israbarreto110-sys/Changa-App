
name: changa_app
description: "App de changas cerca"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_maps_flutter: ^2.7.0
  geolocator: ^11.0.0
  flutter_local_notifications: ^17.2.2
  cloud_firestore: ^5.4.4
  firebase_core: ^3.6.0
  url_launcher: ^6.3.1

flutter:
  uses-material-design: true
