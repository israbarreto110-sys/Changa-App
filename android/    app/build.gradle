plugins {
    id "com.android.application"
    id "kotlin-android"
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace "com.tu_usuario.changa_app" // Asegúrate de tener tu namespace
    compileSdkVersion 34 // O flutter.compileSdkVersion si tu versión lo soporta

    defaultConfig {
        applicationId "com.tu_usuario.changa_app"
        minSdkVersion 21
        targetSdkVersion 34
        versionCode flutterVersionCode.toInteger()
        versionName flutterVersionName
    }
    // ...
}


    defaultConfig {
        applicationId "com.israbarreto110.sys.changa_app"
        minSdkVersion 21
        targetSdk 35
        versionCode flutterVersionCode.toInteger()
        versionName flutterVersionName
    }

    buildTypes {
        release {
            signingConfig signingConfigs.debug
            minifyEnabled false
            shrinkResources false
        }
    }
}

flutter {
    source '../..'
}
